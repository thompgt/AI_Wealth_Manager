# Dependencies for demo.ipynb only.
#
# Split out of requirements.txt because nothing the API or the dashboard
# imports needs a Jupyter kernel or a static image exporter. Installing them
# to run the server buys a slower install and a larger image for a file the
# server never opens.
#
#     pip install -r requirements.txt -r requirements-notebook.txt

-r requirements.txt

# Notebook execution.
nbformat==5.10.4
nbclient==0.11.0
ipykernel==7.3.0

# demo.ipynb sets `pio.renderers.default = "png"`, so plotly renders through
# kaleido. Without it every figure in the notebook raises instead of drawing.
kaleido==1.3.0
